# Environment Configuration Example
# This file shows all available configuration options
# Copy non-sensitive values to .env (which is committed)
# Store sensitive values in GitHub Actions secrets

# ============================================
# GCP Configuration (non-sensitive)
# ============================================
GCP_REGION=us-central1
GAR_LOCATION=us-central1
GAR_REPOSITORY=lean-hub

# ============================================
# Service Names (non-sensitive)
# ============================================
SERVICE_PROJECT_1=project-1
SERVICE_PROJECT_2=project-2-rag
SERVICE_PROJECT_3=project-3

# ============================================
# API Gateway Configuration (non-sensitive)
# ============================================
API_GATEWAY_NAME=lean-hub-gateway
API_GATEWAY_API=lean-hub-api
API_GATEWAY_CONFIG=lean-hub-config
CUSTOM_DOMAIN=hub.yourdomain.com

# ============================================
# Resource Configuration (non-sensitive)
# ============================================
STANDARD_MEMORY=512Mi
STANDARD_CPU=1
STANDARD_MAX_INSTANCES=10
STANDARD_MIN_INSTANCES=0

RAG_MEMORY=16Gi
RAG_CPU=4
RAG_GPU_COUNT=1
RAG_GPU_TYPE=nvidia-l4
RAG_MAX_INSTANCES=10
RAG_MIN_INSTANCES=0

# ============================================
# Security Settings (non-sensitive)
# ============================================
INGRESS_INTERNAL_ONLY=true
ALLOW_UNAUTHENTICATED=true
REQUEST_TIMEOUT=300

# ============================================
# Development Settings (non-sensitive)
# ============================================
LOCAL_PORT=8080
LOG_LEVEL=INFO
DEBUG_MODE=false
DEFAULT_SHARED_LIB_VERSION=v1.0.0

# ============================================
# Monitoring (non-sensitive)
# ============================================
ENABLE_CLOUD_LOGGING=true
ENABLE_CLOUD_MONITORING=true
LOG_RETENTION_DAYS=30

# ============================================
# SECRETS - Store in GitHub Actions Secrets
# ============================================
# DO NOT put actual values here or in .env!
# Add these to GitHub: Settings → Secrets and variables → Actions
#
# Required GitHub Secrets:
# - GCP_PROJECT_ID (your GCP project ID)
# - GCP_SA_KEY (service account JSON key)
# - FIREBASE_API_KEY (from Firebase Console)
# - FIREBASE_PROJECT_ID (from Firebase Console)
# - FIREBASE_AUTH_DOMAIN (from Firebase Console)
# - FIREBASE_STORAGE_BUCKET (from Firebase Console)
# - FIREBASE_MESSAGING_SENDER_ID (from Firebase Console)
# - FIREBASE_APP_ID (from Firebase Console)
#
# For local development, set these as environment variables:
# export GCP_PROJECT_ID=your-project-id
# export FIREBASE_API_KEY=your-api-key
# etc.

# ============================================
# Service Account (for local development)
# ============================================

# Path to your service account JSON key file
# Create this with: gcloud iam service-accounts keys create key.json --iam-account=...
GOOGLE_APPLICATION_CREDENTIALS=./gcp-service-account-key.json

# ============================================
# Firebase Configuration
# ============================================

# Firebase Project ID (usually same as GCP_PROJECT_ID)
FIREBASE_PROJECT_ID=your-firebase-project-id

# Firebase API Key (from Firebase Console > Project Settings)
FIREBASE_API_KEY=your-firebase-api-key

# Firebase Auth Domain
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com

# Firebase Storage Bucket
FIREBASE_STORAGE_BUCKET=your-project.appspot.com

# Firebase Messaging Sender ID
FIREBASE_MESSAGING_SENDER_ID=your-sender-id

# Firebase App ID
FIREBASE_APP_ID=your-app-id

# ============================================
# GitHub Actions Secrets (for CI/CD)
# ============================================

# These should be added to GitHub Repository Secrets
# Settings → Secrets and variables → Actions

# GCP_PROJECT_ID (same as above)
# GCP_SA_KEY (entire JSON content of service account key)
# FIREBASE_TOKEN (optional, for Firebase CLI: firebase login:ci)

# ============================================
# API Gateway Configuration
# ============================================

# API Gateway name
API_GATEWAY_NAME=lean-hub-gateway

# API Gateway API name
API_GATEWAY_API=lean-hub-api

# API Gateway config name
API_GATEWAY_CONFIG=lean-hub-config

# Custom domain (optional)
CUSTOM_DOMAIN=hub.yourdomain.com

# ============================================
# Cloud Run Service Names
# ============================================

# Service name for Project 1
SERVICE_PROJECT_1=project-1

# Service name for Project 2 (RAG)
SERVICE_PROJECT_2=project-2-rag

# Service name for Project 3
SERVICE_PROJECT_3=project-3

# ============================================
# Shared Library Versioning
# ============================================

# Default shared library version for new projects
DEFAULT_SHARED_LIB_VERSION=v1.0.0

# ============================================
# Resource Configuration
# ============================================

# Project 1 & 3 Resources
STANDARD_MEMORY=512Mi
STANDARD_CPU=1
STANDARD_MAX_INSTANCES=10
STANDARD_MIN_INSTANCES=0

# Project 2 RAG Resources
RAG_MEMORY=16Gi
RAG_CPU=4
RAG_GPU_COUNT=1
RAG_GPU_TYPE=nvidia-l4
RAG_MAX_INSTANCES=10
RAG_MIN_INSTANCES=0

# ============================================
# Security & Authentication
# ============================================

# Set to 'true' for internal-only services (recommended)
INGRESS_INTERNAL_ONLY=true

# Allow unauthenticated access (API Gateway handles auth)
ALLOW_UNAUTHENTICATED=true

# ============================================
# Development Settings
# ============================================

# Local development port
LOCAL_PORT=8080

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Enable debug mode (true/false)
DEBUG_MODE=false

# ============================================
# Cost Optimization
# ============================================

# Auto-scale to zero when idle (true/false)
AUTO_SCALE_TO_ZERO=true

# Request timeout in seconds
REQUEST_TIMEOUT=300

# ============================================
# Monitoring & Observability
# ============================================

# Enable Cloud Logging (true/false)
ENABLE_CLOUD_LOGGING=true

# Enable Cloud Monitoring (true/false)
ENABLE_CLOUD_MONITORING=true

# Log retention days
LOG_RETENTION_DAYS=30
